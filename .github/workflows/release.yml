name: Release workflow

# TODO: Maintain floating version tags to allow flexible version pinning,
# so users can reference actions with major (v1) or minor (v1.2) tags.

on:
  push:
    branches:
      - main

permissions: {} # No permissions by default on workflow level

jobs:
  release-please:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Needed to create releases and tags
      pull-requests: write # Needed to create release PRs
    steps:
      - name: Harden the runner (audit all outbound calls)
        uses: step-security/harden-runner@8d3c67de8e2fe68ef647c8db1e6a09f647780f40 # v2.19.0
        with:
          egress-policy: audit

      - name: Run release-please
        uses: googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7 # v5
        id: release
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
          target-branch: ${{ github.ref_name }}

      - name: Debug release-please outputs
        env:
          RELEASES_CREATED: ${{ steps.release.outputs.releases_created }}
          PATHS_RELEASED: ${{ steps.release.outputs.paths_released }}
          RELEASE_OUTPUTS: ${{ toJSON(steps.release.outputs) }}
        run: |
          echo "releases_created: $RELEASES_CREATED"
          echo "paths_released: $PATHS_RELEASED"
          echo "--- Full JSON outputs ---"
          echo "$RELEASE_OUTPUTS"

      - name: Delete release (keep tag only) - zizmor
        if: ${{ steps.release.outputs['actions/zizmor--release_created'] }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ steps.release.outputs['actions/zizmor--tag_name'] }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          echo "Deleting release: $TAG_NAME"
          gh release delete "$TAG_NAME" --yes --cleanup-tag=false --repo "$GITHUB_REPOSITORY"

      - name: Delete release (keep tag only) - bandit
        if: ${{ steps.release.outputs['actions/bandit--release_created'] }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ steps.release.outputs['actions/bandit--tag_name'] }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          echo "Deleting release: $TAG_NAME"
          gh release delete "$TAG_NAME" --yes --cleanup-tag=false --repo "$GITHUB_REPOSITORY"
